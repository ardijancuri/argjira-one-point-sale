# ===========================================
# ARGJIRA POS - BACKEND CONFIGURATION
# ===========================================
# Copy this file to .env and fill in your values

# -------------------------------------------
# DATABASE CONFIGURATION (Supabase Cloud)
# -------------------------------------------
# Get these values from administrator or Supabase Dashboard
DB_HOST=aws-0-eu-central-1.pooler.supabase.com
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres.your_project_id
DB_PASSWORD=your_supabase_password
DB_SSL=true

# -------------------------------------------
# JWT AUTHENTICATION
# -------------------------------------------
# Generate a secure random string (32+ characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=generate_a_secure_random_string_here
JWT_EXPIRES_IN=7d

# -------------------------------------------
# SERVER CONFIGURATION
# -------------------------------------------
PORT=3000
NODE_ENV=production

# -------------------------------------------
# CORS CONFIGURATION
# -------------------------------------------
# URL where the frontend is served from
FRONTEND_URL=http://localhost:3000

# -------------------------------------------
# FISCAL PRINTER CONFIGURATION
# -------------------------------------------
# Service authentication token for the print agent
SERVICE_AUTH_TOKEN=generate_another_secure_random_string

# ZFPLab server settings (Tremol fiscal printer)
ZFPLAB_HOST=localhost
ZFPLAB_PORT=4444

# API URLs
API_URL=http://localhost:3000
CLOUD_API_URL=http://localhost:3000

# Print agent polling interval in milliseconds
PRINT_POLL_INTERVAL=500

# Fiscal printer serial port settings
# Find your COM port in Device Manager
FISCAL_COM_PORT=COM8
FISCAL_BAUD_RATE=115200
